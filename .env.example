# Copy to .env and fill with a PostgreSQL connection. Never commit real credentials.
PGHOST=localhost
PGDATABASE=reconciliation
PGUSER=recon
PGPASSWORD=change-me
PGSSLMODE=disable
PGCHANNELBINDING=prefer
DATABASE_URL='postgres://recon:change-me@localhost:54329/reconciliation?sslmode=disable'
